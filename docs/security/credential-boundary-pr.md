# Credential hardening PR

- deny direct access to common secret material through the default AI shell policy;
- add regression coverage for the denial boundary;
- document the backend-owned credential boundary and remaining hardening work.
