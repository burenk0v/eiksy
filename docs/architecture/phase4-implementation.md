# Phase 4 implementation

The product foundation is deliberately incremental. Sessions and settings already expose stable service APIs; this phase adds transport-neutral and AI-provider-neutral domain contracts without moving secrets or bypassing policy/execution/audit boundaries.
