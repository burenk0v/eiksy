Phase 4 Product Foundation PR.

Adds transport-neutral connection contracts, an AI provider runtime contract, regression tests for both contracts, and architecture documentation. Existing security boundaries, sessions/settings APIs, SSH/SFTP implementations, AI HTTP implementation, policy, approval, execution, and audit paths remain unchanged.
